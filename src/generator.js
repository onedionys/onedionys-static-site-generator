const fs = require('fs');
const path = require('path');
const marked = require('marked');

function generateSite(contentDir, outputDir) {
  // Your site generation logic here
}

module.exports = { generateSite };
