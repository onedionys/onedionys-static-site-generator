const assert = require('assert');
const { generateSite } = require('../src/generator');

describe('Static Site Generator', function() {
  it('should generate HTML files from markdown files', function() {
    // Your test case here
    assert.strictEqual(true, true);
  });
});
